## تعامل با محیط CLI
  // مطالعه manual 
  // استفاده از آپشن های مختلف
## نصب سیستم عامل
  // استاندارهای نصب
  // نحوه پارتیشن بندی
  // مدیریت پکیج های نصب شده
 
## linux Hierarchy file system 
   - directory نقش etc
   - var
   - root v home v tmp
  
## مطالعه کارهای متادول
  // اشنایی با پکیج منیجر مانند apt yum
  // آشنایی و پیاده سازی repository
  // مطالعه lvm
  // resize file system
  // معماری process های در linux
  // نوشتن سرویس در systemd
  // نحوه جمع آوری و ارسال لاگ در لینوکس (rsyslog)
  
